# Setmates — Full product vision

This file is intentionally a placeholder in the MVP repository snapshot.

The live MVP implementation should not pull additional scope from here unless that scope is
also repeated in `docs/SPEC-MVP.md`.
